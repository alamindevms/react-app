import "./Header.css";

const Header = () => {
  return (
    <header className="header">
      <nav>
        <a href="/">Home</a>
        <a href="/about">About Us</a>
      </nav>
    </header>
  );
};

export default Header;
